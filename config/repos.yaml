# Multi-repo readiness (Phase 5)
# Primary runtime repo
primary:
  owner: AgentMindCloud
  name: RepoMind
  default_branch: main

# Future satellite repos (not auto-executed yet)
satellites: []
  # - owner: AgentMindCloud
  #   name: example-satellite
  #   roles: [skills-only]

# Rules
rules:
  allow_cross_repo_comments: false
  allow_cross_repo_prs: false
  require_human_approval_for_satellites: true
